{
  "name": "ChemicalLog",
  "type": "object",
  "properties": {
    "week_start": {
      "type": "string",
      "format": "date",
      "description": "Date log was opened (Monday)"
    },
    "week_end": {
      "type": "string",
      "format": "date",
      "description": "Date log was completed (Friday)"
    },
    "chemicals": {
      "type": "string",
      "description": "JSON array of {chemical_name, unit, start_amount, end_amount}"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "week_start",
    "chemicals"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    }
  }
}