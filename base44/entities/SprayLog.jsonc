{
  "name": "SprayLog",
  "type": "object",
  "properties": {
    "cell_id": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "quantity_liters": {
      "type": "number"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "cell_id",
    "date"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        }
      ]
    }
  }
}